# starship-arch
starship config, os autodetection for icon etc etc


install:

``` curl -o ~/.config/starship.toml https://raw.githubusercontent.com/Yqno/starship-arch/main/starship.toml && sed -i '/^style_clean/d' ~/.config/starship.toml ```
